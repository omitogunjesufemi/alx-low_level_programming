#include <stdio.h>

/**
 * main - main method
 * @argc: count of arguments
 * @argv: array of pointers
 * Return: 1
 */
int main(int __attribute__((unused)) argc, char **argv)
{
	printf("%s\n", *argv);

	return (0);
}
